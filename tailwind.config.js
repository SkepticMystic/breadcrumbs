/** @type {import('tailwindcss').Config} */
module.exports = {
	// NOTE: I removed html and js, since I don't think I'll need that here
	content: ["./src/**/*.{svelte,ts}"],
	theme: {
		extend: {},
	},
	plugins: [],
};
